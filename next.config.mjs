/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "oblivicon.com",
      "static.vecteezy.com",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
